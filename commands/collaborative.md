---
description: Switch to collaborative mode (default) — user and master work together
---

# /collaborative

This is the DEFAULT mode. In collaborative mode:

- User discusses tasks with master session
- Master helps formulate clear task definitions
- User approves decomposition before agents are launched
- User can intervene at any stage
- All decisions require user confirmation

> This mode is currently active by default. Switching modes is not yet implemented.
