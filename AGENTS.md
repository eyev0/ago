# AGENTS.md

This repository contains the `ago` plugin marketplace source.

- Target environment: Codex-compatible agent workflows only.
- This repo is not a standalone library or CLI.
- The plugin payload lives under `plugins/ago/`.
- User-facing install, update, and command documentation lives in `README.md`.
- Claude-specific notes live in `CLAUDE.md`.

Keep this file short. Agents should use it only as a quick repository map, then follow the linked instructions.
